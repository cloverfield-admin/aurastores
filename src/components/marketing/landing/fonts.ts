/**
 * Font-family stacks for the marketing landing (v2). These reference the
 * `next/font` CSS variables registered in `src/app/layout.tsx` and are used
 * from inline `style` objects across the landing components so the design's
 * per-element typography stays 1:1 with the comp.
 */
export const FONT_DISPLAY = "var(--font-schibsted), 'Schibsted Grotesk', sans-serif";
export const FONT_MONO = "var(--font-plex-mono), 'IBM Plex Mono', monospace";
export const FONT_BODY = "var(--font-manrope), 'Manrope', system-ui, sans-serif";
